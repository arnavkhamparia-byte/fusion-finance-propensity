
# Azure OpenAI Foundry (for azure:<deployment> models in pipeline/llm_provider.py)
AZURE_OPENAI_TARGET_URL=
AZURE_OPENAI_API_KEY=
# Optional: only needed if the target URL has no /deployments/<name>/ path
AZURE_OPENAI_DEPLOYMENT=
